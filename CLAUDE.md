# M-FACILITY リポジトリ運用ルール

## リポジトリの構成と役割

| パス | 役割 |
|------|------|
| `React/Projects/` | **自作版** — ユーザーが学習しながら実装する主体 |
| `React/fable-projects/` | **見本（完成形）** — Claude Fable 5 が設計書のみから実装 |
| `React/documents/` | 設計書3点 + 比較評価レポート |
| `PHP/` | 卒業制作のPHP版（リライト元・参照用） |

## 学習ワークフローの運用（Claudeへの指示）

1. **自作版（`React/Projects/`）の実装はユーザー自身が行う。** Claudeは答えのコードを直接書かず、`React/Projects/CLAUDE.md` の対話スタイル（ヒント中心・before/after比較・最大3指摘）に従う。
2. **見本（`React/fable-projects/`）の実装・拡張は、ユーザーから明示的な依頼があったときのみ行う。** その際は `React/documents/` の設計書のみを入力とし、自作版のコードは参照しない。
3. **Phaseの節目などで、見本と自作版を比較評価し `React/documents/04_比較評価レポート_vX.X.md` を更新する。**
   - 評価者（Claude）は見本の実装者と同一モデルであるため、自己評価バイアス対策として全判定にファイルパス・行番号・コマンド出力の根拠を付ける。根拠を示せない優劣判定は書かない。
   - 集計値は表の行から検算してから記載する（v1.0→v1.2で集計ミスを修正した経緯あり）。
   - 版を上げる場合はファイル名の版番号も更新する。
4. **見本のコードを自作版へコピーしない（コピーの提案もしない）。** 差分は「なぜそうするのか」の理解を促すヒントとして提示し、実装はユーザーに委ねる。
